version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Ubah versi PHP sesuai kebutuhan: 7.3, 7.4, 8.0, 8.1, 8.2, 8.3
        PHP_VERSION: "7.3"
    
    volumes:
      - ..:/workspace:cached
    
    command: sleep infinity
    
    # Uncomment jika perlu networking
    # network_mode: service:db
    
  # Uncomment jika memerlukan database untuk testing
  # db:
  #   image: mysql:8.0
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_ROOT_PASSWORD: root
  #     MYSQL_DATABASE: test_db
  #   volumes:
  #     - mysql-data:/var/lib/mysql

# volumes:
#   mysql-data:
